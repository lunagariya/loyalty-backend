import { Router } from 'express';import { asyncHandler } from '../../utils/asyncHandler';import { appUninstalled,customerCreated,orderCancelled,orderCreated,orderPaid,orderRefunded,orderUpdated } from '../../controllers/webhook.controller';export const webhookRouter=Router();webhookRouter.post('/orders/create',asyncHandler(orderCreated));webhookRouter.post('/orders/paid',asyncHandler(orderPaid));webhookRouter.post('/orders/updated',asyncHandler(orderUpdated));webhookRouter.post('/orders/cancelled',asyncHandler(orderCancelled));webhookRouter.post('/orders/refunded',asyncHandler(orderRefunded));webhookRouter.post('/customers/create',asyncHandler(customerCreated));webhookRouter.post('/app/uninstalled',asyncHandler(appUninstalled));
